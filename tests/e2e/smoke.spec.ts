import{test,expect}from"@playwright/test";test("Arabic welcome and protected route",async({page})=>{await page.goto("/");await expect(page.locator("html")).toHaveAttribute("dir","rtl");await expect(page.getByRole("heading",{name:/اهتمام أنسب/})).toBeVisible();await page.goto("/app");await expect(page).toHaveURL(/login/)});test("mobile navigation remains usable",async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto("/");await expect(page.getByRole("link",{name:"ابدأ بخصوصية"})).toBeVisible()});
