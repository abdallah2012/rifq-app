import{requireUser}from"@/lib/supabase/server";import{notFound}from"next/navigation";export default async function Page({params}:{params:Promise<{profileId:string}>}){const{id}= {id:(await params).profileId};const a=await requireUser();if(!a)return null;const{data:p}=await a.client.from("profiles").select("display_name").eq("id",id).maybeSingle();if(!p)notFound();const[{data:c},{data:k},{data:f}]=await Promise.all([a.client.from("cycle_records").select("*").eq("profile_id",id).order("period_start_date",{ascending:false}),a.client.from("daily_checkins").select("*").eq("profile_id",id).order("checkin_date",{ascending:false}),a.client.from("activity_feedback").select("*").eq("profile_id",id).order("feedback_date",{ascending:false})]);return <section><h1>سجل {p.display_name}</h1><div className="grid two"><article className="card" style={{padding:"1.25rem"}}><h2>الدورات</h2>{c?.length?c.map(x=><p key={x.id}>{x.period_start_date}{x.period_end_date?` – ${x.period_end_date}`:""}</p>):<p className="muted">لا يوجد سجل بعد.</p>}</article><article className="card" style={{padding:"1.25rem"}}><h2>الحالات والأنشطة</h2><p>{k?.length??0} حالة يومية · {f?.length??0} تقييم نشاط</p></article></div></section>}
